{
  "schema_version": 1,
  "metrics": {
    "production_loc": 2306,
    "production_files": 42,
    "production_packages": 9,
    "test_files": 11,
    "production_functions": 179,
    "decision_points": 315
  },
  "persistence": {
    "module": "modernc.org/sqlite",
    "entities": [
      "PlayerProfile",
      "MatchRecord",
      "BoardSnapshot",
      "MatchEvent"
    ],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {
      "steps": [
        "register the two active players",
        "start a deterministic board position",
        "complete the match with a final board",
        "query the completed history entry"
      ],
      "test": "TestWorkflowRegisterStartAndComplete"
    },
    {
      "steps": [
        "start an active match from the opening board",
        "submit a legal black move",
        "persist the move event and board snapshot",
        "read the event and snapshot timeline"
      ],
      "test": "TestWorkflowLegalMovePersistsEventAndSnapshot"
    },
    {
      "steps": [
        "start and finish a recorded match",
        "filter history by the white player",
        "restrict results to completed status",
        "verify the matching summary is returned"
      ],
      "test": "TestWorkflowHistoryFilterReturnsCompletedPlayerMatches"
    }
  ]
}
